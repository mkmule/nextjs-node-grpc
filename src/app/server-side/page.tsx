'use server';

const ServerSidePage = () => {
  return (
    <main className="p-2">
      <h5 className="text-sm opacity-70">(this is server side rendering page)</h5>
    </main>
  );
};

export default ServerSidePage;
